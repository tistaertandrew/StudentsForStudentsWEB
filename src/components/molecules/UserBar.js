import AccountCircle from "@mui/icons-material/AccountCircle";
import * as React from "react";

export default function UserBar() {
    return (
        <AccountCircle className={'text-white scale-150 m-1 cursor-pointer'}/>
    )
}